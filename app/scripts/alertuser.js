function alertUser () {
alert("The code works");
}
function writeTable () {
var para = document.createElement("p");
//inserts variable dayweek into paragraph element
var node = document.createTextNode("it's working");
para.appendChild(node);
//changes the day of the week as we create more table headers	
	}